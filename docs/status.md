# Phone Remote status (as of 2026-09-03)

Single source for what is built, what is proven, and what is left. Update it in place; do not append a diary. Scope and gate definitions stay in `docs/iphone_ble_remote_mvp_scope.md` (section 7). Rules and invariants live in `docs/worker_learnings.md`.
Hardware: dliao's iPhone 15 Pro Max (iPhone16,2) on iOS 27.0 beta (24A5380h); Apple silicon MacBook Pro (M1 Max) on macOS 27.0 beta; Xcode 27.0 beta (27A5252f), Swift 6.4.
No secrets, team IDs, UDIDs, QR payloads, keys, typed text, transcripts, or audio ever go in this file.

## 1. Where things stand

- Automated baseline is green. Last full run: shared 28/28, macOS 29/29, iOS simulator 42/42, plus the 2,000-input decoder corpus. First green five-target build and test loop: 2026-09-02.
- The project is generated by `scripts/generate_fallback.py` (XcodeGen is not installed). Six targets: `PhoneRemoteShared`, `PhoneRemote-iOS`, `PhoneRemote-macOS`, and three test bundles. Swift 6 with `SWIFT_STRICT_CONCURRENCY=complete`.
- Signed install and launch on the physical iPhone works (first proven 2026-09-02). The phone currently carries bundle `com.davidliao.phoneremote.ios`, built with `PHONE_REMOTE_BUNDLE_PREFIX=com.davidliao.phoneremote`. Scripts default to `com.example.phoneremote.ios`.
- iPhone log collection (`scripts/logs-phone.sh`, sysdiagnose) is blocked by Xcode 27 `CoreDeviceCLISupport.DiagnoseError error 0`. Workaround: the in-app `IPhoneDebugLog` pulled by `scripts/debug-phone.sh`.
- The Mac app lives at `~/Applications/PhoneRemoteMac.app`, signed locally, Accessibility granted (2026-09-02). It serves a loopback debug API at `http://127.0.0.1:18765/state` and `/health` and writes `/tmp/phoneremote-mac-debug.json`.
- In-app QR pairing was observed end to end on hardware once (2026-09-02, Mac showed `Paired with <phone name>`, Keychain trust record present). Later BLE and reconnect changes broke it several times; fixes landed with tests, but a fresh hardware QR-to-paired run after the last fixes is not recorded.
- The iPhone camera preview was black from 2026-09-02 to 2026-09-03 across 18 code attempts. Root cause was the phone's iOS 27 beta camera pipeline, not the app; a phone restart fixed it. See `docs/postmortem_camera_black_preview.md`. The scanner code was cleaned back to a plain wide camera with 2x zoom.
- Ping and trackpad on hardware were dead after pairing once (Mac ignored a repeated hello). Fixed 2026-09-02 (Mac accepts a new control hello while paired; phone marks paired on finish sent). Physical ping/trackpad result after the fix is not recorded.
- Air mouse is wired end to end (phone clutch, Core Motion, `motionPointerDelta`, Mac pointer injector). Axes inverted, linear gain, default speed 2400, slider 500 to 6000, dead zone 0.0004 rad per sample (was 0.002, which swallowed anything slower than about 11 deg/s), 40 ms coalesced flush. Trackpad has its own speed slider (0.5x to 4x). Both sliders persist in UserDefaults. Physical feel, drift, and 32 px target checks are not recorded.
- Push-to-talk voice typing is implemented as a live IMA ADPCM stream (PRA1 start/data/end frames) into a local `nemo-speech` realtime WebSocket (Swift client, one connection per utterance, no Python or S1-mini), typed through the safe text path. Reworked 2026-09-03 after a skeptical audit: the phone runs capture, 40 ms chunking, ADPCM, encryption and fragmentation on one serial voice queue with a warm audio engine (start frame before the mic, end frame before mic stop, whole-message drops with sequence gaps, interruption and background handling); the Mac streams PCM to the server as frames arrive, commits on end or 1.5 s idle, and types finals in start order (final text measured 103 ms after commit on the live server). Owner then heard clipping at both ends: fixed by keeping the audio session active between presses (play-and-record, mix with others; released on background or interruption), a 150 ms release grace on the phone, and 400 ms of trailing silence before commit on the Mac. `ptt_first_audio.ms` in the phone log measures press-to-first-audio. On hardware the hold once produced `sent=0` because of a stuck `notForeground` flag; fixed with tests. A typed spoken sentence on the Mac has not been observed.
- Trusted reconnect without QR is implemented on both sides with tests. A physical pair-then-relaunch reconnect is not recorded. Every fresh `devicectl install` can wipe the phone's Keychain trust store, forcing a new QR.
- All gate records in `docs/verification/` are `NOT RUN` or `BLOCKED`. `go-no-go.md` says BLOCKED.
- Git: the whole tree was first committed on 2026-09-03 as `checkin`. Commit small changes with real messages from here on.
- `AUD-003` deviates from the backlog: there is no Apple `Speech` framework adapter anywhere; transcription is the local Nemotron server. Treat that as the accepted design unless the owner objects.

## 2. Gate status

Procedures and result templates live in `docs/verification/`. Each row links its file; do not repeat the procedure here. `docs/verification/go-no-go.md` (2026-09-02) is stale: its test counts are 23/14/15 and it does not know about `debug-phone.sh`, the camera fix, or the later BLE/PTT changes. Its gate verdicts are still correct.

| Gate | Status | Evidence | Missing | Record file |
|---|---|---|---|---|
| A Toolchain and device loop | BLOCKED (near pass) | generate/build/test scripts green; signed install and launch pass; Mac launch plus `logs-mac.sh` pass | `logs-phone.sh` fails with Xcode 27 `DiagnoseError error 0` (workaround `debug-phone.sh` is not sysdiagnose); XcodeGen not installed | `docs/verification/gate-a-toolchain.md` |
| B BLE link | NOT RUN | Framing, scheduler, and reassembly tests; Mac discovered and connected to the phone in-app on 2026-09-02 | 30 min 100 Hz soak, p95 RTT <= 50 ms, no stall > 250 ms, Bluetooth off/on recovery | `docs/verification/gate-b-ble.md` |
| C Pairing security | NOT RUN | Handshake tamper/replay/wrong-secret tests, one-time offer tests, Keychain store tests; one owner-observed QR pair | Physical scan/cancel/confirm pass, reconnect/revoke on each side; attack matrix rows are all `TBD` | `docs/verification/gate-c-pairing.md` |
| D Input safety | NOT RUN | Policy, watchdog, retry, release, and framed-input tests against `MockInputEventSink`; `CGEventInputSink` exists; Accessibility granted | Real event posting check, 100 forced disconnects during mouse-down and modifier-down, lock/sleep/pause rejection | `docs/verification/gate-d-input-safety.md` |
| E Air mouse | NOT RUN | Filter fixtures, clutch/reference test, motion-to-Mac integration test | Stationary drift run, clutch recenter, 32 px target selection | `docs/verification/gate-e-air-mouse.md` |
| F Voice path | NOT RUN | PTT gating, chunker, ADPCM round trip, reassembler gap accounting, stream-then-type test | Five-minute run, WAV inspection, gap metrics, concurrent pointer latency, visible transcript | `docs/verification/gate-f-audio.md` |
| Lifecycle (LIFE-001) | NOT RUN | `PhoneLifecycleCoordinator`, `MacLifecycleCoordinator` tests | Every matrix row is `TBD` | `docs/verification/lifecycle-matrix.md` |
| Final decision | BLOCKED | Stale snapshot, verdicts still valid | Gates A to F | `docs/verification/go-no-go.md` |

## 3. Ticket backlog

Status: done = acceptance criteria verified; partial = code and tests exist, required hardware or manual evidence missing; open = little or no code.

| ID | Title | Status | Note |
|---|---|---|---|
| FND-001 | Pin toolchain and device matrix | done | `docs/development_environment.md`; XcodeGen version and bundle prefix still `TBD` |
| FND-002 | XcodeGen workspace and targets | partial | `project.yml` plus `scripts/generate_fallback.py` work; `xcodegen generate` never run because it is not installed |
| FND-003 | CLI build/test/device/launch/log scripts | partial | All scripts exist in `scripts/`; only `logs-phone.sh` is blocked (Xcode tooling) |
| PRO-001 | Protocol models and v1 envelope | done | `Shared/Protocol/ProtocolTypes.swift`, `ProtocolCodec.swift`; 8,192 byte envelope cap |
| PRO-002 | Decoder hardening and fuzz | done | `scripts/fuzz-protocol.sh`, `testDecoderBoundedDeterministicFuzzCorpus` |
| SIM-001 | Deterministic simulated transport | done | `Shared/TestTransport/SimulatedTransport.swift` |
| OBS-001 | Privacy-safe metrics | done | `Shared/Observability/Metrics.swift`; recorder is used in tests only, apps do not emit metrics |
| BLE-001 | GATT service and framing contract | done | `docs/ble_gatt_contract.md`, `Shared/Crypto/BLEGATTContract.swift`, `BLEFraming.swift` (16-byte header, 20-byte minimum) |
| BLE-002 | iPhone peripheral shell | partial | `iPhone/Bluetooth/IPhoneBLEPeripheral.swift`, `CoreBluetoothPeripheralManagerAdapter.swift`; Mac saw the service on hardware but no recorded inspection |
| BLE-003 | Mac central shell | partial | `Mac/Bluetooth/MacBLECentral.swift`, `CoreBluetoothCentralManagerAdapter.swift`; hardware connect observed once, not recorded as a gate check |
| BLE-004 | Fragmentation, reassembly, reliable frames | done | `BLEFragmenter`, `BLEReassembler`, `BLEOutboundScheduler` in `Shared/Crypto/BLEFraming.swift` |
| BLE-005 | 100 Hz soak and latency proof | open | No soak tool or run; `gate-b-ble.md` template only |
| PAIR-001 | One-time QR tokens on Mac | partial | `Mac/Pairing/MacPairingOffer.swift`, `Shared/Crypto/PairingTypes.swift` (`prqr1.` tag, 120 s expiry); injected-clock tests pass; owner-visible QR confirmed |
| PAIR-002 | iPhone QR scan and one confirmation | partial | `iPhone/Pairing/IPhonePairingScanner.swift`; camera live again after phone restart; formal scan/cancel/confirm pass not recorded |
| PAIR-003 | X25519/HKDF authenticated session | done | `Shared/Crypto/PairingHandshake.swift`; tamper, replay, wrong secret, rollback tests |
| PAIR-004 | Persist, reconnect, list, revoke trust | partial | `KeychainTrustedDeviceStore`, `TrustedDeviceManager`; reconnect tests pass; no physical reconnect/revoke run; no revoke UI observed |
| PAIR-005 | Pairing attack and redaction matrix | open | `gate-c-pairing.md` rows all `TBD` |
| SAFE-001 | Pure input-safety state machine | done | `Mac/InputInjection/InputSafetyPolicy.swift` |
| SAFE-002 | Accessibility flow and CGEvent sink | partial | `CGEventInputSink`, `SystemAccessibilityTrust`; permission granted on the installed copy; absent/revoked cases not exercised |
| SAFE-003 | Reliable transitions and 500 ms watchdog | done | `Mac/InputInjection/ReliableInputCoordinator.swift` |
| SAFE-004 | Menu bar, Pause, Mac lifecycle guards | partial | `MacLifecycleCoordinator`, `RemoteControlStatusItemController`, `MacRemoteAppModel`; lock/sleep/logout transitions not observed on hardware |
| PAD-001 | One-finger pointer, two-finger scroll | partial | `iPhone/Trackpad/TrackpadGestureEngine.swift`; zero deltas suppressed, 6 pt tap threshold; Finder/browser check not recorded |
| PAD-002 | Atomic clicks and guarded drag | partial | Tap and two-finger tap wired; `dragEnabled` defaults to false and must stay so until disconnect fault tests pass |
| KEY-001 | Bounded Unicode text entry | partial | `iPhone/Trackpad/KeyboardInput.swift` chunker, `SharedKeyboardProtocolAdapter`; TextEdit matrix not run |
| KEY-002 | Fixed hotkey allowlist | partial | `HotkeyAction` enum (copy, paste, undo, redo, selectAll, escape, return, tab, arrows), `HotkeyPhysicalSequence`; two-layout manual check not run |
| MOT-001 | Pure motion-to-pointer filter | done | `iPhone/MotionPointer/MotionPointerFilter.swift` |
| MOT-002 | Core Motion, clutch, calibration | partial | `MotionPointerSession`, `CoreMotionDeviceProvider`, `FeatureMotionSink` (40 ms flush); Gate E measurements missing |
| AUD-001 | 16 kHz PCM push-to-talk capture | partial | `iPhone/Audio/AudioCaptureSession.swift`, `AudioCaptureModels.swift`; mic engine created lazily on press; physical hold with `sent > 0` not recorded |
| AUD-002 | PCM transport, health, WAV on Mac | partial | `VoicePTTCoordinator` counts frames, samples, and sequence gaps; `AudioHealthIndicatorView`; live path uses ADPCM `VoiceStreamFrame`; no WAV sink; five-minute run not done |
| AUD-003 | Transcription adapter and safe insertion | partial | `NemotronRealtime`, `VoicePTTCoordinator`; no Apple Speech adapter; secure input skips typing; spoken sentence not observed |
| LIFE-001 | Reconnect, lifecycle, sleep/wake, revocation | open | `PhoneLifecycleCoordinator` and Mac lifecycle exist; `lifecycle-matrix.md` rows all `TBD` |
| QA-001 | Fault-injection and coexistence matrix | open | Nothing run |
| QA-002 | Gates A to F go/no-go report | open | `go-no-go.md` exists and says BLOCKED |

Regression guards worth keeping from the acceptance criteria:
- PAD-002: drag stays off by default until forced-disconnect mouse-down tests pass.
- SAFE-003: watchdog is 500 ms; retries are finite; duplicates apply at most once.
- PAIR-004: trusted reconnect always runs a fresh authenticated handshake; a BLE identifier or name is never identity.
- AUD-001: capture starts only from a local press; backgrounding, interruption, and route change stop it.

## 4. Known problems and traps

Build and project
- Xcode 27 rejects `xcodebuild -target ... -derivedDataPath`. Always pass `-scheme`.
- `scripts/generate.sh` deletes and recreates `PhoneRemote.xcodeproj`. Never run two generators at once; they race on `xcshareddata/xcschemes`.
- In the fallback generator, give groups repository-relative paths only. Mixing physical and relative paths produced `Shared/Shared/...` duplicates.
- Shared framework must pin `IPHONEOS_DEPLOYMENT_TARGET=18.0` and `MACOSX_DEPLOYMENT_TARGET=15.0` or Xcode 27 picks a newer SDK and the apps cannot link it.
- Test bundles need `ENABLE_TESTABILITY=YES` on the app plus `TEST_HOST` and `BUNDLE_LOADER`, or they compile and fail to link.
- Both app plists need `CFBundleExecutable=$(EXECUTABLE_NAME)`. Without it the simulator install fails with `MissingBundleExecutable`.
- App module names are `PhoneRemote_iOS` and `PhoneRemote_macOS`. XCTest imports must match.
- The framework install name must be `@rpath/PhoneRemoteShared.framework/PhoneRemoteShared`, and app `LD_RUNPATH_SEARCH_PATHS` must include `@executable_path/Frameworks` (iOS) and `@executable_path/../Frameworks` (macOS). Without both the app dies at launch while `devicectl` still reports launch success.
- The repo lives under iCloud Documents. `DerivedData` there picks up Finder tags that break `codesign`. Use `PHONE_REMOTE_DERIVED_DATA=/tmp/PhoneRemotePhoneInstall` (or any `/tmp` path) for device builds.
- Device builds must set `ENABLE_DEBUG_DYLIB=NO`. An unsigned Xcode debug or preview dylib inside the bundle makes iOS kill the app on open. `install-phone.sh` and `install-mac.sh` already pass it; the installer signs every Mach-O then the app.
- The `.serena/` directory and `scripts/__pycache__/` are tool residue, not project code.

Signing and device
- Signing is opt-in: `PHONE_REMOTE_SIGNING=1` plus `PHONE_REMOTE_DEVELOPMENT_TEAM` (optional `PHONE_REMOTE_CODE_SIGN_IDENTITY`, default `Apple Development`). Missing values fail before any device call. Never commit them.
- Pass the phone as an explicit destination (`platform=iOS,id=$IPHONE_UDID`). A generic device build can pick a profile for a different phone and fail at install.
- `xcodebuild` from a non-GUI shell sometimes cannot see the Xcode account. Recovery used once: sign the built app in place with the local identity and existing profile, then `codesign --verify --deep --strict`, then `devicectl device install app`.
- The phone must be unlocked on screen for DDI. `unlockedSinceBoot=true` is not enough; a locked phone returns CoreDevice 12040. `device info ddiServices` must show `isUsable: true`.
- `security find-identity -v -p codesigning` is the quick check for a local signing identity.
- The installed bundle is `com.davidliao.phoneremote.ios`. Pass `PHONE_REMOTE_IOS_BUNDLE_ID=com.davidliao.phoneremote.ios` to `launch-phone.sh` and `debug-phone.sh`, or `debug-phone.sh` prints `STATE_MISSING`.
- `debug-phone.sh` copies by device name (`PHONE_REMOTE_DEVICE_NAME`, default `dliao's iPhone`), not UDID.
- A fresh `devicectl install` often gives the app a new container and wipes the phone Keychain trust store. Expect a new QR after every reinstall.

Mac app and Accessibility
- macOS grants Accessibility to an exact path and signature. Only `~/Applications/PhoneRemoteMac.app` counts. Never `open` a `/tmp` or DerivedData `PhoneRemoteMac.app`; it looks granted while the live app stays denied. Use `scripts/install-mac.sh`.
- `AXIsProcessTrusted()` stays false for adhoc-signed Debug builds with the debug dylib stub. Sign with the local identity after copying (Finder metadata blocked signing during the build step).
- `MacRemoteAppModel` must call `MacLifecycleCoordinator` and `SafeInputInjector` directly. Do not add a second pause or status boolean.
- Missing `NSBluetoothAlwaysUsageDescription` made the macOS test host die before XCTest attached. iPhone plist needs camera, Bluetooth, motion, and microphone strings.
- The Mac debug server can be disabled with `PHONE_REMOTE_DEBUG_SERVER=0`. It binds 18765 or the next few ports and records the port in `/tmp/phoneremote-mac-debug.json`.
- `scripts/logs-mac.sh` shows unified logs, but the app emits no pairing events there. Use `/state`.

BLE and pairing ordering rules
- The iPhone never appears in macOS Bluetooth Settings. Pairing is in-app only: Mac shows QR, phone scans and confirms, phone advertises, Mac auto-connects. There is no device picker.
- Advertise with `CBUUID(nsuuid:)`. A Foundation `UUID` in `CBAdvertisementDataServiceUUIDsKey` silently drops the service.
- Guard `stopScan`, `stopAdvertising`, `removeAllServices` on `.poweredOn` or Core Bluetooth logs `API MISUSE`.
- After an unpaired connect or discovery failure the Mac must resume scanning. After a ready disconnect it must rescan for trusted reconnect.
- The 3 second advertise pulse must refresh in place. Calling `stopAdvertising` mid-connect drops the link. Pulse is a no-op while publishing, connected, or ready.
- An advertise error must not tear down a live beacon. The Mac keeps its QR offer if hello never started.
- "Waiting for Bluetooth" is not powered-off. Treating it as off killed advertising before it could start.
- The Mac must accept a new control hello even when it believes it is already paired, answer hello while still subscribing, and keep a live session if a later hello arrives. The phone marks itself paired as soon as finish is sent and forwards Mac control writes without a matching subscriber id.
- QR scan never stops Bluetooth. Any advertising pause waits for the camera's `onStarted`. Opening the camera pauses saved-Mac reconnect until Confirm; Cancel resumes it. A QR handshake failure stays on the QR path and never falls back to old trust.
- `KeychainTrustedDeviceStore.allRecords()` must query `kSecReturnAttributes` with `kSecMatchLimitAll`, then fetch each record singly. `kSecReturnData` plus `kSecMatchLimitAll` returns `errSecParam` on macOS and the menu bar shows "Pairing storage is unavailable".
- Keep BLE-ready and authenticated separate in every UI and snapshot. `MacPairingProgress` is presentation only.
- The QR pairing ID is the trust-record device ID on both sides.

Camera and audio
- When a camera or mic feed looks dead, open the built-in Camera app first. If it is dead too, restart the phone. Do not touch app code. `screenCaptured=yes` with nothing mirroring plus constant `luma=16` means iOS muted the feed.
- Scanner: back wide camera, `.high` preset, preview layer attached in init, one output (QR metadata), `automaticallyConfiguresApplicationAudioSession=false`, 2x zoom after configuration. Do not reintroduce a frame tap, deferred-start calls, the virtual triple camera, a SwiftUI `clipShape` over the preview, or a full-screen shell.
- Hold to talk owns the audio session and must restore `SoloAmbient` on stop. The capture session never configures the audio session. The `AVAudioEngine` is created only when a press starts.
- App start marks the mic path backgrounded. Foreground must clear it, or press returns `notForeground` and `sent=0`.
- Pro iPhones cannot focus closer than about 20 cm. The scanner hint says 25 cm.

Concurrency and Swift 6
- Static GATT and framing collections need `Sendable` value types. Add conformances to immutable structs and enums only; no `@unchecked Sendable` over mutable state.
- `kAXTrustedCheckOptionPrompt` is a non-Sendable imported global. Use a literal CFDictionary key.
- Core Motion callbacks arrive off main. `FeatureMotionSink` adds deltas and flushes at most every 40 ms on the same main turn. Do not add a `Task { @MainActor }` hop; it queued without bound and caused growing lag.
- Motion packets are unreliable; drop extra samples instead of queueing. Unreliable follow-on fragments still enqueue so a message is never half sent.
- Shared code stays transport neutral: no Core Bluetooth, Core Graphics, Core Motion, AVFoundation, or Speech types in `Shared/`.

Transcription helper
- `Mac/Transcription/NemotronRealtime.swift` health-checks `nemo-speech serve` on `PHONE_REMOTE_NEMO_PORT` (default 18766) at launch, spawns `~/.local/bin/nemo-speech` with the Nemotron GGUF from `~/.cache/huggingface/hub` when nothing answers, logs to `/tmp/phoneremote-nemo-speech.log`, and stops the child on quit. Each utterance is one `/v1/realtime` WebSocket; audio streams as it arrives and `.end` (or 1.5 s of silence from the phone) commits.
- Transcripts are never logged and never appear in `/state`. Debug counters only: `audioFrames`, `audioSamples`, `audioMissingChunks`, `audioPhase`.

Environment noise that is not a failure
- macOS tests print `com.apple.linkd.autoShortcut` warnings. Simulator prints a Core Motion preference notice. `logs-mac.sh` shows CoreSpotlight and XPC noise. AppIntents metadata warnings appear in builds.

## 5. How to verify

Generate, build, and run all automated suites (simulator picked by UUID from `simctl`):

```bash
./scripts/generate.sh
PHONE_REMOTE_DERIVED_DATA=/tmp/PhoneRemoteBuild ./scripts/build.sh
PHONE_REMOTE_RUN_IOS_TESTS=1 PHONE_REMOTE_DERIVED_DATA=/tmp/PhoneRemoteTests ./scripts/test.sh
PHONE_REMOTE_DERIVED_DATA=/tmp/PhoneRemoteFuzz ./scripts/fuzz-protocol.sh
# Pin a simulator if needed:
# PHONE_REMOTE_IOS_DESTINATION='platform=iOS Simulator,id=<uuid>'
```

Single suite, or list targets:

```bash
xcodebuild test -project PhoneRemote.xcodeproj -scheme PhoneRemoteSharedTests -destination 'platform=macOS' -derivedDataPath /tmp/PhoneRemoteShared CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO
xcodebuild test -project PhoneRemote.xcodeproj -scheme PhoneRemote-macOSTests -destination 'platform=macOS,arch=arm64' -derivedDataPath /tmp/PhoneRemoteMacTests CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO
xcodebuild -project PhoneRemote.xcodeproj -list
```

Phone readiness (keep the phone unlocked on screen):

```bash
xcrun devicectl list devices
xcrun devicectl device info details --device "$IPHONE_UDID"      # Developer Mode Enabled
xcrun devicectl device info ddiServices --device "$IPHONE_UDID"  # isUsable: true
security find-identity -v -p codesigning
```

Signed phone install, launch, and liveness (values stay in the local shell only):

```bash
export IPHONE_UDID=<local-only>
export PHONE_REMOTE_DEVELOPMENT_TEAM=<local-only>
PHONE_REMOTE_BUNDLE_PREFIX=com.davidliao.phoneremote \
PHONE_REMOTE_DERIVED_DATA=/tmp/PhoneRemotePhoneInstall \
PHONE_REMOTE_SIGNING=1 ./scripts/install-phone.sh
PHONE_REMOTE_IOS_BUNDLE_ID=com.davidliao.phoneremote.ios ./scripts/launch-phone.sh
sleep 3; xcrun devicectl device info processes --device "$IPHONE_UDID" | grep -i PhoneRemote
# Optional loader check:
xcrun devicectl device process launch --device "$IPHONE_UDID" --console --terminate-existing com.davidliao.phoneremote.ios
```

Phone debug log (the only working phone log path):

```bash
PHONE_REMOTE_IOS_BUNDLE_ID=com.davidliao.phoneremote.ios ./scripts/debug-phone.sh
# Blocked by Xcode 27 (DiagnoseError error 0), kept for retry on a newer Xcode:
PHONE_REMOTE_PHONE_LOG_OUTPUT=/tmp/PhoneRemotePhysicalLogs ./scripts/logs-phone.sh
```

Mac install to the stable path, live state, and logs:

```bash
PHONE_REMOTE_DERIVED_DATA=/tmp/PhoneRemoteMacInstall PHONE_REMOTE_SIGNING=1 ./scripts/install-mac.sh
./scripts/debug-mac.sh /state      # appPath, BLE state, pairing, accessibility, audio counters
./scripts/debug-mac.sh /health
PHONE_REMOTE_LOG_WINDOW=1m ./scripts/logs-mac.sh
ps -o pid,lstart,command -p "$(pgrep -f PhoneRemoteMac.app)"
```

Strict Swift 6 pure-source type check (used as an extra gate in the log):

```bash
xcrun swiftc -parse-as-library -typecheck -swift-version 6 -strict-concurrency=complete Shared/Crypto/*.swift Shared/Protocol/*.swift Shared/Observability/*.swift Shared/TestTransport/*.swift
```

Manual hardware loop that has never been fully re-run after the last fixes:

1. `install-mac.sh`, then grant Accessibility to `~/Applications/PhoneRemoteMac.app` and click Refresh Accessibility. `/state` must say `granted`.
2. Install and launch the phone app. Keep it in front. Do not hold talk first.
3. Mac menu bar: Show one-time pairing QR. Phone: Scan Mac QR Code, live picture, Confirm. Phone debug should show `trust_save ok=yes`. Mac should leave Looking for iPhone and reach Paired.
4. Tap Ping Mac (must enable), swipe the pad (cursor moves), hold Air Mouse (cursor follows, up/right is up/right).
5. Click a Mac text field, Request microphone access, hold Push to talk, speak, release. Phone debug: `ptt_press.result=started`, `sent > 0`. Mac `/state`: `audioFrames > 0`. Text appears.
6. Quit and relaunch the Mac app. Phone should reconnect without a QR.
